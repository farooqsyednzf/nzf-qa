[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Redirect /api/qa to the function for cleaner URLs
[[redirects]]
  from = "/api/qa"
  to   = "/.netlify/functions/coda-proxy"
  status = 200
